//with the help of list we can insert and delete at any location
//means we can insert at middile and end

//Here we can preserve the insertion

#include<iostream>
#include<list>
#include<bits/stdc++.h>
using namespace std;
int main()
{



    list<myclass>  mylist;
    myclass obj;//creating object
    mylist.push_back(obj);

    mylist.push_back(myclass());

    forward_list<int> mfList;//forwad list was introduced in c++11

    //Inserting in Vector O(n)
    //Accessing the element in vector O(1)

return 0;
}